# Add bills object types
type Bill {
  billLog: [BillLog]
  billPaid: [BillPaid]
}

type BillLog {
  amount: Int
  createdAt: String
}

type BillPaid {
  amount: Int
  createdAt: String
  mode: String
}

type Query {
  bills: [Bill]
}